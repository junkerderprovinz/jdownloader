- #1 **Removed the bottom taskbar that v4.5.0 added.** For a single-window app like JDownloader the taskbar added no real value and did not fit the UI, so it is gone again. Switching between JDownloader, its dialogs and the optional captcha browser still works with Alt+Tab and the openbox desktop menu (middle-click the empty desktop for a window list).
